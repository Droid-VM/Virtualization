{
  "comments": [
    {
      "unresolved": true,
      "key": {
        "uuid": "75f1a180_fae68ab5",
        "filename": "guest/pvmfw/src/memory.rs",
        "patchSetId": 11
      },
      "lineNbr": 82,
      "author": {
        "id": 1943701
      },
      "writtenOn": "2024-12-04T08:58:11Z",
      "side": 1,
      "message": "Hi 😊 As an unsafe Rust reviewer who don\u0027t know anything about the code base here, I\u0027m not sure that I can follow this safety comment.\n\nI would expect the commend to start with the preconditions for `slice::from_raw_parts`: https://doc.rust-lang.org/stable/std/slice/fn.from_raw_parts.html. I think the comment here implies that the range of `fdt_size` elements starting at `fdt as *mut u8` are valid and correct for `from_raw_parts`.\n\nHowever, to see if the safety comment is correct, I have to rely on the comment at `map_data`. Assuming it\u0027s the function introduced in this change, the comment just says\n\n\u003e Map the provided range as normal memory, with R/W permissions.\n\nand this is not enough for me to connect the dots, so to speak 😊\n\nCan you help me by making a clear connection between what `map_data` validates and the preconditions for `from_raw_parts`?\n\nThe same applies to the other `map*` functions.",
      "range": {
        "startLine": 82,
        "startChar": 8,
        "endLine": 82,
        "endChar": 17
      },
      "revId": "a306e76fd886848546b0b2722e8a8cd2374fa1ac",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    }
  ]
}