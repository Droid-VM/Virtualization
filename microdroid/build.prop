# build.prop for microdroid
ro.apex.updatable=true
ro.adb.secure=0
service.adb.listen_addrs=vsock:5555

# Payload metadata partition
apexd.payload_metadata.path=/dev/block/by-name/payload-metadata

# Microdroid doesn't have logd. Instead, use file_logger which
# sends logs directly to the host side via serial console.
ro.log.file_logger.enabled=1
