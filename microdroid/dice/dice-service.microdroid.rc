service dice-microdroid /system/bin/dice-service.microdroid
    class main
    user diced
    group diced
    # The DICE service must not be allowed to restart.
    # If it crashes for any reason security critical state is lost.
    # The only remedy is to restart the device.
    oneshot
    writepid /dev/cpuset/foreground/tasks
