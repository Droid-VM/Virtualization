#!/bin/bash

umask 000
export XDG_RUNTIME_DIR=/tmp/xdg
mkdir -p $XDG_RUNTIME_DIR
export WESTON_TTY=1
weston --tty="$WESTON_TTY" --idle-time=0 --xwayland --shell=kiosk-shell.so --continue-without-input